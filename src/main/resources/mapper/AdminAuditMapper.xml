<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aaa.project.dao.AdminAuditDao">
    <select id="listAll" resultType="aaa.project.entity.Apartment">
        select *from apartmentinfo1
    </select>
    <select id="findById" resultType="aaa.project.entity.Apartment">
        select * from apartmentinfo2 where aptNum=#{newAptNum}
    </select>
</mapper>
